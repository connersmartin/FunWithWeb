
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h3>You're signed in to spotify and can search now!</h3>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <h4><span class="artistSearch">Tempo </span>Search</h4>
        <p>How to use:
        <ol>
            <li class="artistSearch">Enter a Tempo</li>
            <li class="default">Enter Search text (Artist, Album or Song)</li>
            <li class="artistSearch">Enter an artist to build a playlist based off that tempo and the artist you selected</li>
            <li>Click Search</li>
        </ol>
        </p>
        <hr/>
        <div class="form-group">
            <div class="col-md-10">
                @Html.Editor("id") <span></span><input type="checkbox" id="tempoSearch" />Tempo search?
            </div>
            <br>
            <div class="col-md-10 artistSearch">
                @Html.Editor("artist") <span>Artist search</span>
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-10">
                <input type="submit" value="Search" class="btn btn-default"/>
            </div>
        </div>
    </div>
}

@section Scripts{
    <script type="text/javascript">
        $(".artistSearch").hide();
        $("#tempoSearch").click(function () {
            if ($(this).is(":checked")) {
                $(".artistSearch").show();
                $(".default").hide();
            }
            else {
                $(".artistSearch").hide();
                $(".default").show();
            }
        });

    </script>
    }
